[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name="beacon_klipper"
description = "Beacon component for Klipper"
license = { text = "GPL-3.0" }
dependencies = [
  "numpy>=1.16.6",
  "klippy"
]
version = "1.0.2+git.g11832cb"

